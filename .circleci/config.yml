version: 2.1
jobs:
  # build-ros-ws:
  #   docker:
  #     - image: flyvoly/lomp:latest

  #   resource_class: medium

  #   steps:
  #     - checkout
  #     - run:
  #         name: Setup submodules
  #         command: |
  #           chmod 700 .ssh/key
  #           eval `ssh-agent`
  #           ssh-add .ssh/key
  #           git submodule sync --recursive && git submodule update --init --recursive
  #     - run:
  #         name: Build ros packages
  #         command: |
  #           source /opt/ros/melodic/setup.bash
  #           catkin build -j4 px4 mavlink_sitl_gazebo mavros mavros_extras volys_precision_landing robot_localization
  #     - save_cache:
  #         key: precision-landing-{{ .Branch }}-{{ .Revision }}
  #         paths: 
  #           - ./

  # run-precision-landing-test:
  #   docker:
  #     - image: flyvoly/lomp:latest

  #   resource_class: large
    
  #   steps:
  #     - restore_cache:
  #         keys: 
  #           - precision-landing-{{ .Branch }}-{{ .Revision }}
      
  #     - run:
  #         name: Run VTOL Mission
  #         command: |
  #           export SITL_RUN_SH="/root/project/src/px4_firmware/test/rostest_precision_landing.sh"
  #           echo ${SITL_RUN_SH}
  #           bash ${SITL_RUN_SH} precision_landing_test.test

  # run-precision-landing-payload-drop-test:
  #   docker:
  #     - image: flyvoly/lomp:latest

  #   resource_class: large
    
  #   steps:
  #     - restore_cache:
  #         keys: 
  #           - precision-landing-{{ .Branch }}-{{ .Revision }}
      
  #     - run:
  #         name: Run VTOL Mission
  #         command: |
  #           export SITL_RUN_SH="/root/project/src/px4_firmware/test/rostest_precision_landing.sh"
  #           echo ${SITL_RUN_SH}
  #           bash ${SITL_RUN_SH} precision_landing_payload_drop.test

  build-container:
    machine:
      docker_layer_caching: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "66:d2:c5:6a:6e:d7:48:41:e6:31:5c:55:17:70:58:b4"
      - checkout
      - run: git submodule sync --recursive && git submodule update --init --recursive
      - run: |
          echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin

      # build the application image
      - run: docker build -f ./Dockerfile.amd64 -t flyvoly/lomp:${CIRCLE_SHA1:0:7} .

      # deploy the image
      - run: docker push flyvoly/lomp:${CIRCLE_SHA1:0:7}

      # - run:
      #     command: |
      #       docker run -d --name ros-test flyvoly/lomp:${CIRCLE_SHA1:0:7}
      #       sleep 20

      #run precision_landing test
      - run: docker run -it flyvoly/lomp:${CIRCLE_SHA1:0:7} bash /usr/app/src/px4_firmware/test/rostest_precision_landing.sh precision_landing_test.test
      

workflows:
  version: 2

  precision-landing-test:
    jobs: 
      - build-container
  
